# 피자집브레이크타임 Monthly Picks AI 개발 지침

이 파일은 AI 개발 도구가 작업을 시작할 때 가장 먼저 읽어야 하는 프로젝트 지침입니다.

## 작업 전 필수 확인

1. `docs/PRODUCT.md`에서 제품 요구사항과 투표 규칙을 확인한다.
2. `docs/ARCHITECTURE.md`에서 현재 기술 결정과 데이터 구조를 확인한다.
3. `docs/ROADMAP.md`에서 현재 단계와 다음 작업을 확인한다.
4. 실제 코드가 문서와 다르면 임의로 한쪽을 기준 삼지 말고 차이를 보고한다.

## 제품 원칙

- 이 서비스는 밴드 `피자집브레이크타임` 멤버들이 매달 합주곡을 제안하고 결정하는 공간이다.
- 일반 사용자는 별도의 회원가입이나 소셜 로그인 없이 이용한다.
- 관리자가 미리 등록하고 일회용 초대 코드를 발급한 사람만 참여할 수 있다.
- 사용자는 `이름 또는 별명 + PIN`으로 자신이 작성한 곡을 수정하거나 삭제한다.
- PIN 원문은 데이터베이스, 로그, 코드에 저장하지 않는다. 안전한 단방향 해시만 저장한다.
- 관리자는 일반 PIN과 별개의 개별 관리자 암호를 사용한다. 관리자끼리 암호를 공유하지 않는다.
- `OWNER`는 관리자 관리 권한을 포함한 전체 권한을, `ADMIN`은 일상 운영 권한을 가진다.
- 월별 참여자 명단을 기준으로 전원 동의 여부를 판단한다.
- 투표하지 않은 상태와 싫어요는 서로 다른 상태로 취급한다.
- 관리자 기능과 일반 참여자 기능의 권한을 분리한다.

## 구현 원칙

- TypeScript의 `strict` 설정을 유지하고 불필요한 `any`를 사용하지 않는다.
- Next.js App Router 구조를 유지한다.
- 서버에서 처리해야 하는 PIN 검증과 권한 확인을 브라우저 코드에 맡기지 않는다.
- 데이터 변경 API는 입력값 검증과 권한 확인을 모두 수행한다.
- UI 컴포넌트는 작고 명확하게 나누되, 실제로 재사용되지 않는 추상화는 만들지 않는다.
- 모바일 화면과 키보드 사용을 기본적으로 지원한다.
- 버튼과 입력창에 접근 가능한 이름과 오류 안내를 제공한다.
- 새 라이브러리는 기존 도구로 해결하기 어려울 때만 추가하고 이유를 설명한다.
- 비밀값은 `.env`에 두며 Git에 커밋하지 않는다.

## 데이터와 보안

- 곡 수정·삭제 시 등록자의 신원을 서버에서 다시 확인한다.
- PIN 비교는 해시 검증으로 수행하며 응답에서 PIN 관련 정보를 노출하지 않는다.
- 동일 인물의 중복 투표를 데이터베이스 제약 조건으로 방지한다.
- 최초 PIN 설정과 PIN 초기화에는 한 번만 사용할 수 있는 만료형 초대 코드를 사용한다.
- PIN과 관리자 암호의 반복 실패를 제한하고, 성공 시 유효기간이 있는 서버 세션을 발급한다.
- 사용자가 입력한 문자열은 길이와 형식을 검증한다.
- 관리자 암호나 데이터베이스 키를 클라이언트 번들에 포함하지 않는다.
- 투표 단계에서는 참여자 명단을 변경하지 않는다.
- 투표 단계에서 등록 단계로 되돌릴 때 기존 투표를 모두 초기화하고 운영 기록을 남긴다.
- 마감된 월은 다시 열거나 수정하지 않는다. 오류가 있으면 무효 처리 후 새 투표방을 만든다.

## 변경 시 함께 해야 할 일

- 요구사항이 바뀌면 `docs/PRODUCT.md`를 수정한다.
- 기술 또는 데이터 구조가 바뀌면 `docs/ARCHITECTURE.md`를 수정한다.
- 작업을 완료하거나 우선순위가 바뀌면 `docs/ROADMAP.md`를 수정한다.
- DB 스키마를 변경하면 Drizzle 마이그레이션을 생성하고 함께 검토한다.
- 기능을 구현한 뒤 최소한 `npm run lint`와 `npm run build`를 실행한다.
- 사용자 동작이나 규칙이 추가되면 해당 테스트도 추가하거나 갱신한다.

## 수정하지 말아야 하는 영역

다음은 자동 생성되므로 직접 편집하지 않는다.

- `node_modules/`
- `.vinext/`
- `.wrangler/`
- `dist/`

`build/`, `worker/`, `vite.config.ts`는 배포 기반 설정이다. 기능 구현에 꼭 필요한 경우가 아니라면 수정하지 않는다.

## 완료 보고 형식

작업을 마치면 다음 내용을 짧게 보고한다.

1. 무엇을 변경했는지
2. 중요한 제품 또는 기술 결정
3. 어떤 검증을 실행했는지
4. 남은 문제나 사용자가 결정해야 할 사항
